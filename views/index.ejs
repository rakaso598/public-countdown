<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | 카운트다운</title> 
    
    <link rel="stylesheet" href="/css/style.css">
    <meta name="target-date" content="<%= targetDate %>"> 
</head>
<body>
    <h1><%= h1Title %></h1>
    
    <% if (isAdmin) { %>
        <p class="admin-link-note"><a href="/admin">⚙️ 관리자 설정 페이지로 이동</a></p>
    <% } %>

    <div class="visitor-display">
        현재 접속자: <span><%= visitorCount %></span> 명
    </div>

    <div id="countdown" class="countdown-display">
        <p>카운트다운을 시작합니다...</p>
    </div>
    
    <div class="button-container">
        <a href="<%= buttonLink %>" target="_blank" class="info-button">
            <%= buttonText %>
        </a>
    </div>

    <p class="note"><%= note %></p>
    <p class="note"><%= subNote %></p>
    
    <script src="/socket.io/socket.io.js"></script> 
    <script src="/js/script.js"></script>

</body>
</html>